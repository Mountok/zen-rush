# ZenRush - Рекомендации активностей

Мобильное веб-приложение для рекомендаций активностей на основе настроения, времени, бюджета и погоды.

## 🚀 Быстрый старт

### Предварительные требования
- Node.js 18+ 
- Бэкенд API (см. `API_DOCUMENTATION.md`)

### Настройка API
Убедитесь, что бэкенд запущен и доступен по адресу `http://localhost:8080`

Подробная информация о бэкенде находится в файле `API_DOCUMENTATION.md`

### Запуск фронтенда

1. **Установите зависимости:**
```bash
npm install
# или
yarn install
```

2. **Запустите dev сервер:**
```bash
npm run dev
# или
yarn dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

## 🔐 Аутентификация и роли

### Система ролей
- **user** - обычный пользователь (просмотр активностей, избранное)
- **moderator** - модератор (управление активностями + права пользователя)
- **admin** - администратор (полные права)

### Тестовые аккаунты
При первом запуске автоматически создается пользователь-админ:
- **Username:** `admin`
- **Password:** `admin123`
- **Role:** `admin`

Для тестирования можно использовать этот аккаунт или зарегистрировать новый с любой ролью.

## 📱 Функциональность

### Главная страница
- **Гибкие фильтры** (настроение, время, количество людей, бюджет)
- **Переключатель погоды** - можно отключить учет погоды
- **Анимированные иконки настроения** с пульсацией
- **Сброс отдельных фильтров** - фильтровать по одному или нескольким критериям
- **Автоматическое определение погоды** по геолокации
- **Аутентификация пользователей**

### Рекомендации
- **Умная фильтрация** - только активные фильтры учитываются в поиске
- **Отображение количества людей** в карточках активностей
- **Получение активностей** с учетом выбранных фильтров
- **Добавление в избранное** с анимацией
- **Оценка активностей** звездочками
- **Автоматическое добавление** в историю просмотров

### Профиль
- Просмотр избранных активностей
- История просмотров
- Отображение роли пользователя
- Управление аккаунтом

### Админ-панель (только для moderator/admin)
- Полное управление активностями (CRUD операции)
- Создание новых активностей с полной формой
- Редактирование существующих активностей
- Удаление активностей с подтверждением
- Встроенная документация API
- Таблица со всеми активностями
- Фильтрация и поиск

## 🛠 Технологии

- **React 18** - Основной фреймворк с Concurrent Features
- **Material-UI (MUI) v5** - UI компоненты с Glassmorphism дизайном
- **React Router v6** - Маршрутизация с Protected Routes
- **Axios** - HTTP клиент с JWT interceptors
- **Vite** - Сборщик и dev сервер
- **JWT** - Аутентификация с ролями пользователей

## 📋 API Endpoints

Подробная документация API находится в файле [`API_DOCUMENTATION.md`](./API_DOCUMENTATION.md)

### Основные эндпоинты:
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход в систему
- `GET /api/activities` - Получение активностей с фильтрами
  - `time` - время в часах
  - `people_count` - количество людей
  - `min_budget`, `max_budget` - диапазон бюджета
  - `mood` - настроение
  - `weather` - погода (sunny/cloudy/rainy/any)
- `POST /api/activities` - Создание активности (moderator/admin)
- `PUT /api/activities/:id` - Обновление активности (moderator/admin)
- `DELETE /api/activities/:id` - Удаление активности (moderator/admin)
- `GET /api/favorites` - Избранное
- `POST /api/favorites/:id` - Добавить в избранное
- `DELETE /api/favorites/:id` - Удалить из избранного
- `GET /api/history` - История просмотров
- `POST /api/history/:id` - Добавить просмотр

## 🔧 Конфигурация

### Настройка API URL:
В файле `src/services/api.js` можно изменить базовый URL API:
```javascript
const API_BASE_URL = 'http://localhost:8080/api';
```

### Переменные окружения:
Создайте файл `.env` в корне проекта:
```env
VITE_API_URL=http://localhost:8080/api
```

## 🐛 Отладка и тестирование

### Проверка подключения к API:
1. Откройте DevTools (F12)
2. Перейдите на вкладку Network
3. Попробуйте войти в систему
4. Проверьте, что запросы к API проходят успешно

### Тестирование JWT токенов:
Откройте файл [`test_jwt.html`](./test_jwt.html) в браузере для:
- Регистрации пользователей с разными ролями
- Тестирования JWT токенов
- Декодирования токенов
- Проверки API endpoints

### Тестирование API:
Откройте файл [`test-api.html`](./test-api.html) для тестирования основных API endpoints.

### Логи приложения:
Откройте DevTools (F12) и перейдите на вкладку Console для просмотра логов приложения.

### Админ-панель:
- Войдите как admin/moderator
- Перейдите в админ-панель через навигацию
- Используйте встроенную документацию для справки

## 📱 Мобильная оптимизация

Приложение оптимизировано для мобильных устройств:
- Адаптивный дизайн
- Touch-friendly интерфейс
- Фиксированная навигация снизу
- Оптимизированные жесты
- Responsive админ-панель

## 🔧 Структура проекта

```
src/
├── components/          # React компоненты
│   ├── AdminDocs.jsx   # Документация для админа
│   ├── AdminRoute.jsx  # Защищенный маршрут админа
│   ├── ActivityCard.jsx
│   ├── Navbar.jsx
│   └── ...
├── pages/              # Страницы приложения
│   ├── AdminPanel.jsx  # Админ-панель
│   ├── Home.jsx
│   ├── Profile.jsx
│   └── ...
├── services/           # API сервисы
│   ├── api.js         # Основной API клиент
│   └── userService.js # Сервис для работы с пользователями
└── ...
```

## 🚀 Развертывание

### Сборка для продакшена:
```bash
npm run build
```

### Развертывание на Vercel/Netlify:
1. Подключите репозиторий к платформе
2. Установите переменные окружения
3. Настройте API URL для продакшена
4. Деплой автоматически запустится при push в main ветку

## 🆕 Что нового в последней версии

### v3.0 - Гибкие фильтры и улучшенный UX
- ✅ **Гибкая система фильтров** - можно отключать отдельные фильтры
- ✅ **Переключатель погоды** - поиск без учета погодных условий
- ✅ **Фильтр по количеству людей** - выбор для 1, 2, 4, 6+ человек
- ✅ **Анимированные иконки настроения** с пульсацией и изменением цветов
- ✅ **Умная фильтрация** - только активные фильтры передаются в API
- ✅ **Кнопки сброса фильтров** - быстрое управление поиском
- ✅ **Отображение количества людей** в карточках активностей
- ✅ **Улучшенная отладка** - подробные логи для разработчиков

### v2.0 - Админ-панель и система ролей
- ✅ **Система ролей пользователей** (user, moderator, admin)
- ✅ **Полноценная админ-панель** для управления активностями
- ✅ **CRUD операции** для активностей (создание, редактирование, удаление)
- ✅ **Встроенная документация** для администраторов
- ✅ **Защищенные маршруты** с проверкой прав доступа
- ✅ **JWT токены** с информацией о роли пользователя
- ✅ **Тестовый инструмент** для проверки API и токенов
- ✅ **Обновленная навигация** с динамическим отображением админ-панели
- ✅ **Улучшенный профиль** с отображением роли пользователя

### v1.0 - Основной функционал
- ✅ Рекомендации активностей по настроению, времени, бюджету и погоде
- ✅ Система избранного и истории просмотров
- ✅ Аутентификация пользователей
- ✅ Мобильная оптимизация
- ✅ Интеграция с бэкендом

## 📄 Лицензия

MIT License
